---
#
# This is the canonical configuration for the `README.md`
# Run `make readme` to rebuild the `README.md`
#

# Name of this project
name: terraform-aws-security-buckets

# Tags of this project
tags:
  - aws
  - terraform
  - terraform-modules
  - security-buckets

# Categories of this project
categories:
  - terraform-modules/CICD

# Logo for this project
#logo: docs/slalom-logo.png

# License of this project
license: "APACHE2"

# Copyrights
copyrights:
  - name: "Slalom, LLC"
    url: "https://slalom.com"
    year: "2019"

# Canonical GitHub repo
github_repo: jameswoolfenden/terraform-aws-security-buckets

# Badges to display
badges:
  - name: "Build Status"
    image: "https://api.travis-ci.com/JamesWoolfenden/terraform-aws-security-buckets.svg?branch=master"
    url: "https://travis-ci.com/JamesWoolfenden/terraform-aws-security-buckets"
  - name: "Latest Release"
    image: "https://img.shields.io/github/release/JamesWoolfenden/terraform-aws-security-buckets.svg"
    url: "https://github.com/JamesWoolfenden/terraform-aws-security-buckets/releases/latest"

related:
  - name: "terraform-aws-codebuild"
    description: "Making a Build pipeline"
    url: "https://github.com/jameswoolfenden/terraform-aws-codebuild"

# Short description of this project
description: |-
  This module is to create a standard place to keep your secure file objects that you need in your account. Its supposed to be a basic step, until its replaced by something more sophisticated.
  Follow the exampleA for the implementation.
# How to use this project
usage: |-
  Include this repository as a module in your existing terraform code:

  ```hcl
  module "security-buckets" {
    source                  = "JamesWoolfenden/security-buckets/aws"
    version                 = "0.0.2"
    s3_bucket_force_destroy = "${var.s3_bucket_force_destroy}"
    key_names               = "${var.key_names}"
    common_tags             = "${var.common_tags}"
  }
  ```

include:
  - "docs/targets.md"
  - "docs/terraform.md"

# Contributors to this project
contributors:
  - name: "James Woolfenden"
    github: "jameswoolfenden"
